# Staging Environment Configuration
# Django Settings
SECRET_KEY=your-staging-secret-key-change-this-in-production
DEBUG=False
ALLOWED_HOSTS=staging.example.com,localhost,127.0.0.1
SITE_URL=https://staging.example.com
MULTI_TENANCY_ENABLED=False
DJANGO_ENV=staging

# Database
DB_NAME=saascrm_staging_db
DB_USER=saascrm_user
DB_PASSWORD=saascrm_password
DB_HOST=db
DB_PORT=5432
DATABASE_URL=postgresql://saascrm_user:saascrm_password@db:5432/saascrm_staging_db

# Email/OAuth (staging values)
EMAIL_HOST=smtp.example.com
GOOGLE_CLIENT_ID=your-staging-google-id
GITHUB_CLIENT_ID=your-staging-github-id

# Frontend
NEXT_PUBLIC_API_URL=https://staging.example.com/api

# Deployment Secrets (for GitHub Actions)
STAGING_SSH_KEY=your-ssh-key
STAGING_USER=ubuntu
STAGING_HOST=staging.example.com
STAGING_PROJECT_PATH=/opt/DjangoCRM
STAGING_DB_CONTAINER=djangocrm-db
STAGING_DB_USER=saascrm_user
STAGING_URL=https://staging.example.com
